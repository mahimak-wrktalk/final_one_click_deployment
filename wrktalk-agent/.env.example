# WrkTalk Agent v4.2 Configuration Example
# Copy this file to .env and update with your values

# ============================================
# Database Configuration (REQUIRED)
# ============================================
WRKTALK_AGENT_DB_HOST=localhost
WRKTALK_AGENT_DB_PORT=5432
WRKTALK_AGENT_DB_NAME=wrktalk
WRKTALK_AGENT_DB_USER=postgres
WRKTALK_AGENT_DB_PASSWORD=your_password_here
WRKTALK_AGENT_DB_SSL_MODE=prefer

# ============================================
# Deployment Type (REQUIRED)
# ============================================
# Options: 'kubernetes' or 'docker'
WRKTALK_AGENT_DEPLOYMENT_TYPE=kubernetes

# ============================================
# Kubernetes Settings (if DEPLOYMENT_TYPE=kubernetes)
# ============================================
WRKTALK_AGENT_KUBE_NAMESPACE=wrktalk
WRKTALK_AGENT_HELM_RELEASE_NAME=wrktalk
WRKTALK_AGENT_HELM_TIMEOUT=10m

# ============================================
# Docker Compose Settings (if DEPLOYMENT_TYPE=docker)
# ============================================
WRKTALK_AGENT_COMPOSE_PROJECT_NAME=wrktalk
WRKTALK_AGENT_COMPOSE_WORKING_DIR=/tmp/wrktalk

# ============================================
# Agent Settings (OPTIONAL)
# ============================================
WRKTALK_AGENT_POLL_INTERVAL=30
WRKTALK_AGENT_HEARTBEAT_INTERVAL=60
WRKTALK_AGENT_MAINTENANCE_MODE_HANDLER=nginx
WRKTALK_AGENT_LOG_LEVEL=INFO

# ============================================
# Notes:
# ============================================
# - SMTP configuration is loaded from deployment_config table in database
# - Admin emails are loaded from admin table in database
# - Artifacts are stored in release_artifact table (BYTEA column)
# - No Backend HTTP URL or MinIO configuration needed in v4.2
